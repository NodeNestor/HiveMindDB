# HiveMindDB integration for AgentCore
# Add these to your .env file or docker-compose environment:

# HiveMindDB cluster URL (required to activate HiveMindDB MCP tools)
HIVEMINDDB_URL=http://hivemind-1:8100

# Set memory provider to hiveminddb (optional — controls which memory backend is active)
MEMORY_PROVIDER=hiveminddb
